{
    "version":"0.2.0",
    "configurations": [
        {
            "name": "Debug Local",
            "type": "Ruby",
            "request": "launch",
            "program": "${workspaceRoot}/bin/rails",
            "args": [
                "server"
            ],
            "cwd": "${workspaceRoot}",
            "showDebuggerOutput": true,
            "env": {
                "RAILS_ENV": "development"
            }
        }
    ]
}